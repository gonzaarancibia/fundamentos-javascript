// Primeros pasos: Consola y Sintaxis básica

// Variables:


// Primeros pasos: Consola y Sintaxis básica

// Variables:

// Crea una variable que guarde un tipo de dato: number
let numero;

// Crea una variable que guarde un tipo de dato: string
let texto;

// Crea una variable que guarde un tipo de dato: boolean
let esVerdadero;

// Crea una variable con el nombre constante, q no se pueda reasignar un valor (cambia el valor de null a lo q quieras)


// Operaciones básicas
// Realiza las siguientes operaciones y guarda los resultados en variables:
let suma;
let resta;
let multiplicacion;
let division;

// Concatenación de strings
let nombre;
let saludo;

// Uso de typeof
let tipoNumero;
let tipoTexto;
let tipoBoolean;
let tipoConstante;

// Uso de let y const
let variableCambiante;

module.exports = {
    numero,
    texto,
    esVerdadero,
    constante,
    suma,
    resta,
    multiplicacion,
    division,
    nombre,
    saludo,
    tipoNumero,
    tipoTexto,
    tipoBoolean,
    tipoConstante,
    variableCambiante
};
